#include <stdio.h>
#include <math.h>
int main ()
{
    double x,y;
    printf("x= ");
    scanf("%lf",&x);
    if (x <= 0)
    y = (pow(x,3) - 5) / (x - 1);
    else 
    if (0 < x && x <= 7);
    y = 74 / 7;
    if (x > 7)
    y = sqrt(x * x + 1);
printf ("%lf", y);
return 0;
}